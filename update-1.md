## Update

# Models
- List of providers has to be collected from Principal Commercial Providers
- Has to retrieve all models available at the moment if refreshed, if there is a new model must be inserted
- Has to retrieve all LLM benchmarks 
- Has to write log when deprecate a model as NOTIFY
- Has to write log when find a new model as NOTIFY
- If refresh is started we need to see a log on system. In the frontend you could advise in settings with a "running" icon. Consider also to advise in other page on same point where you advise that models are updated.
- You have to trace errors when refresh failing notify when it happens and go forward.

# PROJECT
- User could switch Active Project based on the project chosen on Dashboard page

# COST CALCULATION
- PREMIUM seems that is so higher with fictional data

# INTERVIEW - New Project page
- User could choice provider to filter models (Multiple)
- User could choice model to filter (Multiple)
- User could choice a mix provider and models (Multiple) if a model is of one provider you could not choice other model of the same provider
- PREFERRED MODEL on New Project is multiple choice input box that has to suggest dynamically providers and/or models

# USER
- Implement RBAC for Project.
- Implement Group
